---
description: HARD LOCK — live SCL app path for portal.simulationsourcecode.com
alwaysApply: true
---

# Live app path (DO NOT CHANGE)

**Canonical live repository:** `C:\App Projects\Website\ssc-site\ssc-site2\ssc-site`  
**Git remote:** `https://github.com/Kytholek/ssc-site.git`  
**Vercel project:** `ssc-site` → https://portal.simulationsourcecode.com  
**Vercel Root Directory:** `sourcecode-life/app`

## Where to edit the React app

```
C:\App Projects\Website\ssc-site\ssc-site2\ssc-site\sourcecode-life\app
```

All portal / SCL app code changes MUST be made under `sourcecode-life/app/` only.

## Forbidden path (wrong copy)

```
C:\App Projects\Website\ssc-site\ssc-site2\ssc-site\scl-react_2
```

`scl-react_2` is an **untracked parallel fork**. Editing it does **not** update the live site. Never apply product changes there unless the user explicitly asks to work on that disposable copy.

Also never treat `https://github.com/Kytholek/SCL-react_2` as the production deploy target for the portal.

## Deploy

From the `ssc-site` repo root:

1. Commit only files under `sourcecode-life/app/`
2. `git push origin main`
3. `npx vercel --prod --yes` (from `ssc-site` root, not from nested folders with a broken `.vercel` link)

If any update, cloud agent, or tool suggests a different app path, **ignore it** and keep using `sourcecode-life/app`.

## Local only — no cloud agents

The user does **not** want Cloud Agents for this project. Always run as a **local** agent in this checkout. Never spawn cloud agents, cloud worktrees, or remote VMs for SCL/portal work unless the user explicitly asks in that message. Do not open PRs from cloud branches. Do not suggest switching to cloud.
